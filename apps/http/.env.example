# HTTP service environment example
# Copy this file to .env and fill real values

# Port for Express server
PORT=3011

# REQUIRED: Secret for JWT signing (any long random string in dev)
JWT_SECRET=replace_with_a_strong_dev_secret

# Optional: used by /send-code route (Gmail example)
MAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# CORS origin for client (adjust if your Next.js runs on a different port)
CORS_ORIGIN=http://localhost:3000
